These are P1's original synthetic test fixtures ("Sample BIS Product
Standard A/B/C - Not an official BIS document"), moved out of
data/processed/ so embed.py's glob("*_chunks.json") no longer picks
them up. Real content now exists (see data/processed/real_kb_chunks.json,
generated by integration/ingest_real_documents.py) and replaces these
for actual answers.

Kept here (not deleted) since they're still useful for P1's own unit
tests of the retrieval/reranking pipeline in isolation - just no longer
part of the live knowledge base.
